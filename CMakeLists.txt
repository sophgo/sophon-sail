cmake_minimum_required(VERSION 3.2)
project(sophon-inference)
if (NOT DEFINED BUILD_TYPE)
    set(BUILD_TYPE "pcie")
endif()
if("${BUILD_TYPE}" STREQUAL "windows")
    include(build_win.cmake)
else()
    include(build_unix.cmake)
endif()
